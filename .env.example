CURSEFORGE_API_KEY=

# Optional. With a key, download counts and changelogs use the official CurseForge API.
# Without it, both still load at build time via a public CurseForge API proxy.

# Live CMS (GitHub mode) — see README
# KEYSTATIC_GITHUB_CLIENT_ID=
# KEYSTATIC_GITHUB_CLIENT_SECRET=
# KEYSTATIC_SECRET=
